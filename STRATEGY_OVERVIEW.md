# 当前星网决策模型：思路与模块说明

本文以 `team_submission/starnet_model.py` 和同目录的 `strategy_priors.json` 在 2026-09-17 的代码为准，介绍当前主方案。`starnet_model_interleaved.py` 是另一条选择性探索实验分支，不是本文描述的默认流程。

## 一句话概括

先扫描完整网络；用与结算过程相同形式的图传播模拟计算每个节点的终局影响；在预算内比较屏蔽、切边和游说的最终预计得分。Prompt 与节点响应能力未知时，一边正常游说一边获取反馈，更新估计，再重新规划。前期可以只在复制的“影子网络”里预演结构操作，待方案稳定后才实际执行。

## 整体流程

```text
加载先验 → 探索全图 → Prompt 未知期 → Prompt 锁定
                                  ↓
             更新节点/人格 r ← 正常游说获得反馈
                                  ↓
               影子结构规划 ↔ 游说获取更多信息
                                  ↓
                执行结构方案 → 逐轮比较结构动作与游说 → 停机
```

每次 `step()` 通常只执行一次真实环境动作。例外是影子方案提交时，代码会顺序兑现其中的多个结构动作。预算、节点属性和图结构在模型内维护镜像，实际动作的成功与反馈以环境 API 为准。

## 1. 探索与本地图

模型先从 ID 1 开始扫描。已扫描节点暴露出的邻居构成探索前沿，前沿有节点时优先顺藤摸瓜；前沿为空时，在 `1..TOTAL_NODE_CAP` 内继续找尚未扫描的 ID。这使不连通分量也能被发现。当前主模型默认 `TOTAL_NODE_CAP=50`，探索目标是全图，而不是估计何时提前停扫。

扫描结果写入 `nodes`（当前 `w`、人格、剩余游说次数）和 `adj`（邻接表）；已消失或无效的 ID 放在 `dead`。此阶段只侦察，不实施屏蔽、切边或游说。

作用：结构干预是不可逆的，全图信息能避免把尚未发现的连接误判成桥、割点或低度节点。代价是 50 个有效节点需要约 25 点精神力和 50 个执行步。

## 2. 终局收敛模拟与节点影响系数

固定一张图后，模型用 50 轮同步平均模拟观点传播：

\[
w_i^{(t+1)}=\frac{w_i^{(t)}+\sum_{j\in N(i)}w_j^{(t)}}{d_i+1}.
\]

若最终分数是所有存活节点的终值之和，则固定拓扑 \(T\) 下可写为：

\[
F(T,w)=\sum_i y_i(T)w_i,
\qquad y(T)=(M_T^{\mathsf T})^{50}{\bf 1}.
\]

`influence(adj)` 求的就是 \(y_i\)：节点初始观点增加 1，最终总分随之增加多少。屏蔽或切边改变了拓扑，也会改变所有相关节点的 \(y_i\)，因此每个不同拓扑要重新计算。代码以拓扑位图作为缓存键，并用局部缓存和全局 LRU 减少重复计算。

这里的线性换算对**固定拓扑和已知观点变化**是精确的；当 Prompt 强度、节点 \(r\) 尚未确定时，输入的是估计值，决策仍有不确定性。

## 3. 固定拓扑下的最优游说尾部

设 Prompt 估计强度为 \(p\)，节点 \(i\) 的估计响应系数为 \(r_i\)，第 \(k\) 次游说的衰减为 \(c_k\in\{1,0.5,0.25\}\)。一次游说的预计终局增益是：

\[
g_{i,k}=y_i(T)\,p\,r_i\,c_k.
\]

`_terminal_plan()` 枚举所有节点尚可进行的游说，按 \(g_{i,k}\) 排序，取剩余预算可负担的前 \(\lfloor B/2\rfloor\) 项，加到当前拓扑的终局分上。同一节点收益逐次衰减，因此排序会自然遵守“先游说第一次，再考虑第二次”的顺序。

这一步解决的是：**如果结构不再变化，剩余精神力全部用于游说，能得到多少分？** 结构候选和影子方案都通过它计算可比较的终局价值。它没有把游说永远固定为下一步动作；运行时只根据当前反馈执行并重新规划。

## 4. Prompt 未知期：边游说边判断

初始暂以 Prompt 1、强度 `INITIAL_PROMPT_DELTA=15` 规划。`PROMPT_BEST_PRIOR` 给三个 Prompt 最优的初始概率；`PROMPT_EFFECT_MEANS` 给“最佳、次优、最差”三个名次的效果均值。每次正常游说后，代码利用观察到的 \(\Delta w/c_k\)，结合该人格 \(r\) 的当前分布，更新各种 Prompt 名次排列的后验概率。

测试节点的选法不是随机找边缘节点：`_select_safe_prompt_probe()` 会在多种 Prompt 强度和人格 \(r\) 情景中生成“预算内预计会游说的节点清单”，优先选在各清单中都出现、但平均排名相对靠后的未测试节点。目的在于减少未知 Prompt 的试错损失，同时让这次操作仍有正常游说价值。若严格交集为空，则退回到覆盖情景最多的节点。

当某个已测试 Prompt 成为最优的后验概率达到 `PROMPT_LOCK_PROBABILITY=0.8`，或没有下一项可测 Prompt 时，锁定候选并更新 `prompt_delta`。锁定前的观测主要用于识别 Prompt；锁定后再将锚点观测转成节点 \(r\) 估计。

在 Prompt 未知期，也允许一种例外：若同一个结构首动作在弱、中、强 Prompt 情景下都最优，且所有情景的收益都超过下限，`_robust_structure_before_prompt()` 可以先执行这个稳健动作。它避免把“必须先确定话术”当成硬性阶段限制。

## 5. 节点与人格响应系数 r

已游说节点优先使用自身 `r_belief`；未游说节点使用其人格的当前 `persona_r`。Prompt 已校准时，一次反馈可换算为：

\[
\widehat r_i=\frac{\Delta w_i}{p\,c_k}.
\]

同一节点重复游说时，观测对该节点取平均；在人格均值里该节点始终只占一份。人格均值从先验均值与真实节点观测共同计算；样本达到五份后会去掉一个最大值和一个最小值。`R_PRIOR_MEAN_STRENGTH` 控制先验均值在当前宇宙中相当于多少份虚拟节点样本，代码计算时会将它四舍五入为整数。

`R_PRIOR_STD` 代表同一人格中尚未游说节点的预计 \(r\) 波动。`_persona_r_uncertainty()` 将该先验方差与当前宇宙样本方差收缩合并，`_persona_r_distribution()` 再构造低、中、高三个情景，供 Prompt 判断、测试选点和游说信息价值使用。这里的分布是用于决策的近似，并非已知的官方真实分布。

## 6. 游说的信息价值

游说同时带来两类效果：立即改变目标的 \(w\)，以及揭示该节点 \(r\)，从而改善后续分配。`_estimate_communication_with_information()` 会比较“按当前估计执行并固定后续决策”与“观察新 \(r\) 后重新规划”的价值。

当前配置为 `COMM_INFORMATION_FORMULA='old'`：以当前均值下的静态终局分为基线，把按三点 \(r\) 情景重新规划产生的正向差值作为信息收益，再乘 `COMM_INFORMATION_WEIGHT`。代码还保留 `evsi` 口径用于实验；它使用“观测前固定同一游说尾部”的期望收益作基线。默认的 `old` 是历史测试选出的策略口径，属于启发式；不要把它理解为数学上严格的真实期望收益。

## 7. 屏蔽、切边与结构搜索

屏蔽候选是当前 `w < SHIELD_W_THRESHOLD` 的存活节点；切边候选是至少有一端 `w < 0` 的现存边。这里的阈值只负责**筛候选**，不是操作收益公式。屏蔽消耗 5 点，切边消耗 3 点；镜像图上执行后，代码用新的拓扑、观点和剩余预算重新调用 `_terminal_plan()` 得到终局预计分。

`_search_intervention_plan_from_state()` 用 Beam Search 搜索多步结构组合。当前默认参数是：深度 `3`、每层最多保留 `15` 个屏蔽分支与 `15` 个切边分支、全局 Beam 宽 `30`。每个候选叶子都接最优游说尾部；零结构动作的根状态也参与比较，因此允许提前停止结构操作。此处是**有限宽度搜索**，并不保证找到所有可能结构序列中的全局最优。

结构动作与游说都比较预计终局总分。结构动作的成本已从剩余预算扣除，后续少做几次游说的机会成本已体现在终局分里，无需再除一次操作成本。

## 8. 影子规划与实际执行

开启 `ENABLE_SHADOW_PLANNING` 时，先在复制的网络中构建一条完整结构方案，并预留这条方案所需预算。Prompt 未确定时只做较短预览，避免用未经校准的收益反复做昂贵的完整搜索。

Prompt 锁定后，影子方案通过滚动深度搜索产生：每次选当前深度搜索的首个结构动作，在**影子图**执行，再在新影子图上规划下一步。因此深度 3 限制的是每次前瞻的步数，不是整条影子方案最多只能有 3 个结构动作。

在方案真正提交前，模型比较“立即执行影子方案”与“先真实游说一次、获取反馈后重建方案”的预计终局分。游说选点同时考虑真实图和影子图，影子方案连续相同时提高影子图权重。Prompt 已锁定且最终影子拓扑连续稳定 3 轮，或继续等待的收益不够时，按顺序一次性执行结构方案。此后改用普通滚动规划：每回合重新比较结构计划与一次游说，再执行下一步。

影子规划的目的在于推迟不可逆结构动作，让早期游说先提供信息；其效果依赖 Prompt 和 \(r\) 估计，也依赖方案稳定判定，不保证每张图都优于立即执行。

## 9. 可选 LLM 策略顾问与 CaseVO

当前 `ENABLE_LLM_STRATEGY=False`，默认决策由代码完成。CaseVO 中注册侦察兵和执行官角色；若打开 LLM 开关，还注册策略顾问。顾问只看图摘要、代码候选和当前计划，提出额外的屏蔽或切边序列；代码先检查动作合法性，再按同一终局评分器验证，只有比代码计划高出 `LLM_PLAN_MIN_GAIN` 才采用。

因此 LLM 是可选的候选来源，不掌控最终收益判定。`config.json` 定义角色说明；打开 LLM 时还需要相应提示词文件及框架环境。

## 10. 跨宇宙先验文件

`team_submission/strategy_priors.json` 位于模型同目录。模型载入时、每个新宇宙实例初始化时都会读取它；成功读取的同名先验覆盖 Python 文件中的默认值。读取失败则使用代码默认值。文件目前启用了 `cross_seed_evolution.enabled=true`，代码会尝试把新观测写回该文件，供随后运行的宇宙使用；如果官方环境不保留可写文件、隔离进程或写入冲突，运行会回退到当前已加载的先验。

文件中的两组值要区分：

| 字段 | 用途 |
| --- | --- |
| `r_prior.base_personas` | 跨宇宙收缩估计的初始均值和标准差基准 |
| `r_prior.personas` | 下一宇宙实际加载的人格 \(r\) 均值与标准差 |
| `r_prior.mean_strength / std_strength` | 初始基准相当于多少统计权重；当前均为 `10` |
| `prompt_prior.base_best_probability` | Prompt 最优概率的初始基准 |
| `prompt_prior.best_probability` | 下一宇宙实际加载的 Prompt 最优概率 |
| `prompt_prior.strength` | 初始 Prompt 概率的总伪票数；当前为 `5`，不是话术效果强度 |
| `cross_seed_evolution.r_observations_per_persona` | 单宇宙每种人格最多收集多少个跨宇宙用 \(r\) 样本；当前为 `20` |
| `cross_seed_evolution.prompt_update_weight` | 单宇宙 Prompt 后验的总软票权重；当前为 `0.5` |
| `calibration` | 已累积的宇宙记录和统计量 |

对于 \(r\)，同一宇宙的若干节点先汇成该人格的一个均值，跨宇宙时各宇宙等权；方差还计入宇宙内节点差异。对于 Prompt，每个宇宙锁定后最多提交一次最优概率后验作为软票，而不是把模型选出的 Prompt 当成已知真值。文件更新通过锁文件和临时文件替换完成。

目前 `strategy_priors.json` 中的 `calibration` 计数仍为零，所以当前加载的是初始基准值。文档描述的是代码能力与现有配置；能否在官方多种子进程之间持续累积，取决于评测运行方式和文件写权限。

## 11. 评估时看什么

`local_test.py` 负责单种子本地运行和最终分计算，`benchmark_model_configs.py` 负责同批种子的 A/B 对比。比较策略时除了均分，建议关注每个种子的动作序列、屏蔽与切边数量、Prompt 最终选择、剩余预算、预计终局分与实际分的差距，以及新方案在哪些图上改变了首个结构动作。

做严格配对 A/B 时，应从同一份 `strategy_priors.json` 初态分别启动 A、B，或关闭跨种子写入；否则先运行的一侧可能改变后一侧的先验。模型中的先验、有限 Beam 和信息收益估计都可能影响策略，即使收敛评分器在已知图上计算准确，也不能据此断言整个决策过程已经全局最优。
